import { ForTimesDirective } from '~/app/shared/directives/for-times.directive';

describe('ForTimesDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ForTimesDirective();
    expect(directive).toBeTruthy();
  });
});
